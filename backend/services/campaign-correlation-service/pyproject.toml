[project]
name = "cybershield-campaign-correlation-service"
version = "0.1.0"
requires-python = ">=3.11"
description = "CYBERSHIELD AI campaign-correlation-service."

[tool.pytest.ini_options]
testpaths = ["tests"]
