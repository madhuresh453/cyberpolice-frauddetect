[project]
name = "cybershield-ai"
version = "0.1.0"
description = "Backend and AI workspace configuration for CYBERSHIELD AI."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend", "ai"]
