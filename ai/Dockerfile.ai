FROM python:3.11-slim

WORKDIR /app

COPY requirements.gateway.txt /app/requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY ai_gateway.py /app/

EXPOSE 8000

CMD ["uvicorn", "ai_gateway:app", "--host", "0.0.0.0", "--port", "8000"]
