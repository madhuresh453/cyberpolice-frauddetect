# DATABASES

MongoDB Atlas is the only active database for CYBERSHIELD AI.

The database name must be:

```text
cyber-police
```

Runtime code reads the connection string from:

```text
MONGODB_URI
```

Legacy relational databases are not used in this project.

## Phase Status

- Phase 1: directory created.
- Current: MongoDB Atlas-only backend integration.

